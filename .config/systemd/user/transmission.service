[Unit]
Description=Transmission BitTorrent Daemon
After=network.target

[Service]
Type=notify
ExecStart=/usr/bin/transmission-daemon -f --log-error -c %h/Downloads
ExecReload=/bin/kill -s HUP $MAINPID

[Install]
WantedBy=multi-user.target
